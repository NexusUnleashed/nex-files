# Introduction

Clientside trigger handler

```js
add = ({
    regex,
    action,
    id = crypto.randomUUID(),
    tags = [],
    enabled = true,
    once = false,
    duration = false,
    fg = false,
    bg = false,
    lines = 0,
    onRemove,
    onDisable,
    onEnable,
  })
```
